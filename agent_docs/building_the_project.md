# Building the project

Drafted by an AI agent, verified by a human. Do not trust a command below until you have run it yourself from the repo root.

## Prerequisites

[TODO: identify what must be installed or running before the project builds. Name exact versions only where the repository pins them, cite the source for each requirement, and note any verified OS-specific caveats.]

## Build commands

[TODO: document the repository commands that build the app, the artifacts they produce, and where those artifacts land. Account for every supported app variant without guessing defaults.]

## Run the suite

[TODO: document the canonical OS-specific repository command for running the UI suite, everything that must already be running, and the required host and port. Use repository evidence to explain why this command is canonical instead of a direct Gradle test task.]

## Verify it worked

[TODO: document how to prove that tests actually executed and how to find each supported result or report. Distinguish a successful execution from a green command that ran zero tests.]
