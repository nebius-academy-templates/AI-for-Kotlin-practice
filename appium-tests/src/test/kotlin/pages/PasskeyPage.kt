package pages

/** Element catalog for the passkey-offer screen. */
object PasskeyPage {
    val title = Element("passkey_title")
    val skipButton = Element("passkey_skip_button")
    val createButton = Element("passkey_create_button")
}
